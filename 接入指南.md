# 接入指南

> 这是**分享给同事时的唯一入口文档**。看完这一份就能接入，不必先读其它文件。
>
> 本仓库是一套可被多项目引用的 **Element Plus 设计系统 / 组件库**：
> - `design-spec/` —— 设计系统源头（令牌 + 组件覆盖层 + 业务组件 + 规范文档）
> - `demo/` —— 演示页，它本身就是本组件库的一个「引用方」，展示各组件的标准效果

---

## 你属于哪种情况？

| 场景 | 你要做的 |
|---|---|
| **A. 直接在本仓库里干活**（改设计系统、写 demo） | 只需 [第 0 步](#0-拿到代码) + [跑起 demo](#跑起-demo)，其余全部已配好 |
| **B. 在自己的业务项目里用这套设计系统** | 需要走完 [第 1~4 步](#b-在自己的业务项目里接入)，**第 2 步漏了主色会变蓝、第 3 步漏了规则不生效，且都不报错** |

---

## 0. 拿到代码

**必须用 `git clone`，不要用 zip / 网盘传文件夹。**

```bash
git clone <仓库地址>
```

> **为什么强调这条**：仓库里有个 `.claude/` 目录（自动防护 hook，见下文）。它是**隐藏目录**，
> 很多打包工具、聊天软件传文件夹时会默默跳过点开头的目录。
> **丢了不会有任何报错**——只是防护静默失效，看起来一切正常，直到写出违规代码也没人拦。
>
> 确实只能传压缩包时，解压后跑一次下面的自查命令确认 `.claude/` 还在。

### 环境要求

- **Node.js** —— 跑 demo 用
- **jq** —— 防护 hook 依赖它解析 JSON。macOS 通常自带；没有则 `brew install jq`
  > 缺 `jq` 时 hook 会静默失效（同样是「看起来正常但没在保护」），所以下面给了自查命令

### 自查（30 秒，建议拿到代码后先跑一次）

```bash
cd <仓库目录>

# ① 防护 hook 在不在
test -f .claude/settings.json && echo "✓ .claude 存在" || echo "✗ .claude 丢了！重新 git clone"

# ② jq 在不在
command -v jq >/dev/null && echo "✓ jq $(jq --version)" || echo "✗ 缺 jq，请 brew install jq"

# ③ 端到端实测：模拟写一个 2 选项的下拉（规则要求用 Radio），看 hook 拦不拦
jq -n '{tool_input:{file_path:"/x/demo/a.vue",new_string:"<el-select><el-option label=\"左\"/><el-option label=\"右\"/></el-select>"}}' \
  | bash .claude/hooks/guard-control-selection.sh 2>/dev/null \
  | jq -r 'if .hookSpecificOutput.permissionDecision=="ask" then "✓ hook 正常工作" else "✗ hook 未生效" end'
```

三条都 ✓ 就可以开工了。

---

## 跑起 demo

```bash
cd demo
pnpm install
pnpm run dev
```

> ⚠️ **本项目用 pnpm，不要用 npm。** 依赖树由 `demo/pnpm-lock.yaml` 锁定
> （`node_modules/.pnpm/` 是 pnpm 特有的存储结构）。
> 没装 pnpm：`npm i -g pnpm`。

demo 是**标准效果的唯一参照**：只要按下面方式正确接入，你项目里的显示效果与 demo 分毫不差。

### 装不上 / 报奇怪的错？先确认用的是 pnpm

在已有 pnpm 依赖树的项目里跑 `npm install`，会报一个**与真实原因毫无关系**的错：

```
npm error Cannot read properties of null (reading 'matches')
```

它既不提 pnpm、也不提源，很容易被误判成「网络连不上」而去折腾 registry——
**换源、加超时全都没用，因为根本不是网络问题**。改用 `pnpm install` 即可。

> 顺带一提：公司内网源 `depend.iflytek.com` 确实有时会 SSL 握手失败
> （`SSL_ERROR_SYSCALL`）。但那是**另一回事**，且 pnpm 装不动时才需要考虑；
> 遇到上面那条 `Cannot read properties of null` 时先别怀疑网络。

---

## A. 直接在本仓库里干活

**无需任何配置。** 用 Claude Code 打开仓库根目录即可——设计规范和防护 hook 都会自动加载。

只有一件事要知道：**组件样式的唯一数据源在 `design-spec/el-theme/components/`**。
改组件外观一律改那里，**不要**在 demo 的 `<style scoped>` 里覆盖（那叫「局部私货」，会脱离源头、不再同步）。
完整铁律见根 `CLAUDE.md`。

---

## B. 在自己的业务项目里接入

四步，**缺一不可**。第 1 步给你**外观**，第 2 步定**主色**，第 3 步给你**规则**，第 4 步是用组件。

### 第 1 步：引入样式三层（顺序固定，错了覆盖会失效）

在你项目的入口文件（`main.ts`）里：

```ts
import 'element-plus/dist/index.css'                        // ① EP 基础
import '<path>/design-spec/design-token/index.scss'          // ② 设计令牌
import '<path>/design-spec/el-theme/index.scss'              // ③ 组件覆盖层
```

范本见 `demo/src/main.ts`。

**只引这两个 index 入口**，内部几十个组件文件由 index 汇总，不要逐个引用。

> ⛔ **严禁「拷贝式」接入**：不要把 `design-token/` 或 `el-theme/` 的 scss 拷一份进你自己的仓库。
> 拷贝 = 脱离源头，源头更新它不动。只能通过引用（相对路径 / npm 包 / submodule / monorepo）指向同一份源头。

### 第 2 步：注册品牌色（⚠️ 不做的话主色是**蓝的**，而且不报错）

设计系统的 `brands.scss` **只提供 `register-brand` mixin，不预注册任何品牌**——注册是接入方的职责。
**不注册 → `--iflyv-accent-6` 走回退分支 `var(--iflyv-brand-6, var(--iflyv-geekblue-6))` → 落到 geekblue 蓝**，
于是主按钮、选中态、hover 全是蓝的。**这一步漏了不会有任何报错，只能靠眼睛看出来。**

**① 在你项目里建一个 scss 注册品牌**（如 `src/styles/brand.scss`）：

```scss
@use '<path>/design-spec/design-token/css/brands.scss' as *;

// 品牌绿：主色 = 系统 Green-6 #23B283，亮/暗色板原样取自设计系统 Green 阶梯
@include register-brand(
  "green",
  (#EBF7F1, #C1E6D4, #93D9BB, #6ACCA5, #45BF92, #23B283, #158C68, #0A664E, #034032, #011A15),
  (#182522, #19372e, #1d483a, #1e5e49, #207d5e, #229b73, #41af86, #66c39e, #8fd3b6, #bee2d0)
);
```

**② 在 `main.ts` 里、三层样式之后引入它**：

```ts
import './styles/brand.scss'   // 品牌色注册（必须在三层样式之后）
```

**③ 在 `index.html` 的 `<html>` 上声明用哪个品牌**：

```html
<html lang="zh-CN" data-theme="light" data-brand="green" data-font-size="m">
```

> ⚠️ **`data-brand` 的值必须是你注册过的名字。** 写一个没注册过的名字（包括 `default`）
> 等同于没注册——静默回退成蓝色。
>
> 要用自己的品牌色：用 `@ant-design/colors` 的 `generate(base)` 生成 10 级亮色板、
> `generate(base, { theme:'dark', backgroundColor:'#1A1A1A' })` 生成暗色板，换掉上面两组色值即可。
> **色板必须用算法生成，不要手调单个色阶**——10 阶之间的关系是算法保证的，
> 手改会破坏阶梯均匀性，导致 hover / active / 禁用态失衡。
>
> 范本见 `demo/src/styles/global.scss`（demo 自己也是这么注册的）。

### 第 3 步：引入规则（⭐ 最容易漏，漏了等于没接设计系统）

在**你自己项目根**的 `CLAUDE.md` 里加一行：

```
@../xiaoya3.0设计规范/design-spec/CLAUDE.md
```

（路径按你的接入方式改；npm 包接入则为 `@node_modules/<包名>/design-spec/CLAUDE.md`）

**为什么这一行是必须的**：第 1 步只让你拿到**外观**。
不加这行，你项目的 Claude Code **看不到任何规则**——不知道做弹窗要用 `el-dialog`、
不知道表单要套 form-pattern、不知道 2 个选项该用 Radio 而不是 Select、不知道时间要走 `formatTime`。
它只会拿到样式，然后凭手感发明布局。

加了这行，Claude Code 启动时会顺着 `@` 把整份规范拉进上下文，
自动命中「任务 → 必读细则」触发表（做表单/表格/弹窗时自动去读对应规则）。

> 规则单一数据源在设计系统里，**源头更新 → 所有已接入的项目自动生效**，你不用改任何东西。
>
> 范本：本仓库根 `CLAUDE.md` 自己就是这么接的，可直接照抄改路径。

### 第 4 步：用组件

**基础组件**直接用 Element Plus 原生组件即可，样式已被覆盖层接管：

```vue
<el-button type="primary">主操作</el-button>
```

**业务组件**从统一入口 import：

```ts
import { PageFrame, StepBar, Breadcrumb, DataTable, AiButton, UserAvatar, SearchMini }
  from '<path>/design-spec/components'
```

### 接入后同样要守的一条

**不要在自己项目的 scoped 样式里覆盖组件外观**（字号/字重/颜色/边框/圆角/选中态…）。
需要调整外观 → 回设计系统改源头，改一次所有项目同步。
scoped 只放「纯本页排版留白」。

---

## 验收：给自己的项目打个规范分

写完页面后，跑一次评分脚本，看有没有偏离规范：

```bash
# 设计系统与你的项目同级时
node ../xiaoya3.0设计规范/scripts/audit-page.mjs ./src

# 也可以只扫某几个文件
node ../xiaoya3.0设计规范/scripts/audit-page.mjs "src/views/**.vue"

# 给 CI / 其它工具消费
node ../xiaoya3.0设计规范/scripts/audit-page.mjs ./src --json
```

输出是**违规清单 + 合规分**，每条都给出「在哪一行、该怎么改、判据出自哪份规范」。
退出码 0/1，可直接进 CI。

**评分口径**：

- 分级 **必须**(权重 3) / **应该**(2) / **建议**(1)，得分 = 通过权重 ÷ 适用权重 × 100
- **85 分合格**；但**只要有一条「必须」未通过即判不合规**，与总分无关
- 报告末尾的「需人工确认」项机器判不了（如「弹窗标题要写清操作对象」），**不计入分数**，需人工逐条核

**确有理由的例外**：在该行上方写注释声明，评分器会跳过。理由必须写清楚——
这是给规则开的口子，写明白才能在日后复核时判断它是否仍然成立。

```vue
<!-- audit-ignore 品牌色自定义浮层需承载输入框+按钮，dropdown 放不了、dialog 过重 -->
<el-popover ...>
```

> 💡 **懒人入口**：demo 的「评判标准 → Checklist 检查清单」页右上角有个**「复制评分指令」**按钮，
> 点一下复制，粘到你项目的 Claude Code 窗口，CC 就会自己跑评分并按报告修。

**接进自己项目的构建**（推荐只警告不阻断）：

```jsonc
// package.json
"scripts": {
  // 构建末尾打印评分报告，`|| true` 让它不中断构建
  "audit:page": "node ../xiaoya3.0设计规范/scripts/audit-page.mjs ./src",
  "build": "vite build && npm run audit:page --quiet || true"
}
```

> 为什么不当门禁：评分器只覆盖静态可查的部分，误报虽已修过多轮但不可能归零，
> 卡住构建的代价通常大于漏检。要当门禁请去掉 `|| true`，并先在自己项目上跑几轮确认误报可控。

**能力边界**：评分器只覆盖「能静态查出来的」那部分（当前 87 条条目里 66 条可自动检测）。
**跑到 100 分 ≠ 页面设计得好**——信息密度、视觉层级、文案是否得体这些仍须人工审。

> 📖 **完整用法见 [`scripts/README.md`](./scripts/README.md)**：三种使用场景、怎么读报告、
> 误报如何声明豁免、各脚本职责、能力边界。

---

## 关于自动防护 hook

`.claude/hooks/` 下有两个 PreToolUse hook，随仓库分发，**克隆即生效，无需安装、无需授权**。
（项目级 hook 与你个人 `~/.claude/settings.json` 里的 hook 是**合并**关系，不会互相覆盖。）

| hook | 拦什么 |
|---|---|
| `guard-reinvent-components.sh` | **手撸组件库已有之物**（自拼浮层当下拉、自写遮罩、自造菜单条目…） |
| `guard-control-selection.sh` | **组件选对了类、选错了个**（2 个选项用了 Select 而非 Radio、纯图标按钮漏 tooltip、Dialog 用了非档位宽度…） |

**为什么需要它们**：规范写得再全也挡不住「没去读」。真实翻车都是同一个模式——
看到一个形态就直接挑组件写下去，中间没有停顿去查规则。文字规则拦不住，只有 hook 能。

**被拦下时不要顺手放行**：先回答「这形态在组件库里叫什么」，再读 `references/` 对应段（**尤其是「反例」段**，
反例常常直接写着你正要犯的错）。详见 `.claude/hooks/README.md`。

> ⚠️ **这两个 hook 只在本仓库内生效**（它们注册在本仓库的 `.claude/` 里）。
> 你在自己的业务项目里写代码时它们不会跑——所以**场景 B 更依赖第 3 步那行 `@`**，那是你唯一的规则通道。

**本地临时关闭**：在自己的 `.claude/settings.local.json`（已被 git 忽略）里覆盖，
不要直接改 `.claude/settings.json`——那是团队共享的。

---

## 想深入了解

> 下表路径**相对本仓库根目录**（给人翻阅用）。`design-spec/CLAUDE.md` 里那张「任务→必读」触发表的路径则**相对 `design-spec/` 目录**（给 CC 读用），两者差一层前缀，别混淆。

| 我想知道… | 看这里 |
|---|---|
| 完整设计哲学、最高铁律、「任务→必读」触发表 | `design-spec/CLAUDE.md` |
| 令牌怎么选（颜色/字体/间距/圆角/阴影/层级） | `design-spec/references/foundations.md` |
| 某个 EP 组件的用法规范 | `design-spec/references/component-interaction.md` |
| 表单/表格/工具栏/搜索/选择/弹窗等布局模式 | `design-spec/references/patterns/` |
| 时间、数字等文案怎么写 | `design-spec/references/copywriting/` |
| 业务组件层的公约 | `design-spec/components/CLAUDE.md` |
| 防护 hook 的细节 | `.claude/hooks/README.md` |
