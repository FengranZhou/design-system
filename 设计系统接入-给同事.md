# 设计系统接入指引

> 给拿到 `design-system-main.zip` 的同事。**按顺序走完 5 步即可**,大约 15 分钟。
>
> 这个包我已经完整校验过:338 个文件与源仓库逐字节一致,无缺失。

---

## 关于这个包

因为公司禁止把代码上传 GitHub、内网也访问不了 GitHub,所以走的是 zip 交接:

- 这份包**不含 `.git`**,是一个干净起点 —— 你 `git init` 后推到**公司 GitLab**,以后这套设计系统就在公司 GitLab 上维护
- 之前的提交历史留在原作者本机存档,不影响你使用

⚠️ **先别急着 `git init`,请先走完第 0 步。**

---

## 第 0 步:解压后先自查(2 分钟,别跳过)

解压后进入目录,在 **Git Bash** 里跑(不是 PowerShell,原因见第 3 步):

```bash
cd design-system-main

# ① 防护 hook 在不在
test -f .claude/settings.json && echo "✓ .claude 存在" || echo "✗ .claude 丢了,请重新要包"

# ② jq 在不在(hook 依赖它)
command -v jq >/dev/null && echo "✓ jq $(jq --version)" || echo "✗ 缺 jq,见第 3 步"

# ③ 端到端实测:模拟写一个 2 选项的下拉,看 hook 拦不拦
jq -n '{tool_input:{file_path:"/x/demo/a.vue",new_string:"<el-select><el-option label=\"左\"/><el-option label=\"右\"/></el-select>"}}' \
  | bash .claude/hooks/guard-control-selection.sh 2>/dev/null \
  | jq -r 'if .hookSpecificOutput.permissionDecision=="ask" then "✓ hook 正常工作" else "✗ hook 未生效" end'
```

**三条都 ✓ 才继续。**

### 为什么这步不能跳

`.claude/` 是**隐藏目录**(点开头),存放的是自动防护 hook —— 它会在写出违规代码前拦下来。

微信/钉钉传**文件夹**时会默默跳过隐藏目录。**丢了不会有任何报错**,只是防护静默失效,看起来一切正常,直到写出问题代码也没人拦。

> 如果你是把 zip 解压后再转发给别人,对方也要重跑这三条。**传 zip 文件本身最安全。**

### 另外确认一下中文文件名

Windows 解压 GitHub 的 zip,中文文件名有时会变乱码。确认根目录这三个文件名正常:

- `接入指南.md` ← **最重要,这是完整文档**
- `设计系统结构讲解.html`
- `双击这里即可打开预览.command`(这个是 macOS 用的,Windows 忽略)

乱码的话换 **7-Zip / Bandizip** 重新解压。

---

## 第 1 步:清掉一个多余文件

```bash
rm demo/.claude/settings.local.json
```

这是原作者的个人本地配置,误提交进来的,不影响运行但没必要带上。

---

## 第 2 步:推到公司 GitLab

```bash
git init
git add -A
git commit -m "chore: 初始化 xiaoya3.0 设计系统"
git remote add origin <公司GitLab地址>/design-system.git
git push -u origin main
```

### ⭐ 推完必须补装 git hooks(zip 方式特有,容易漏)

```bash
sh .githooks/install.sh
```

**为什么必须**:仓库自带一个 `pre-push` 钩子,它保证 `scripts/catalog.json` 与设计系统保持一致。这个文件是「复制到 CC 使用」扩展面板的数据源 —— **推了旧的,所有人的扩展面板就是旧的,而且没有任何警告**。

正常 `git clone` 时这步由别的流程覆盖,但 zip 方式没有 `.git`,必须手动补。

### 可以顺手删掉 vercel.json

```bash
git rm vercel.json && git commit -m "chore: 移除外网部署配置"
```

它指向 GitHub + Vercel 部署,在公司 GitLab 上没有意义。

---

## 第 3 步:Windows 环境准备(⚠️ 最容易踩)

### 必须装 jq

防护 hook 依赖 `jq` 解析 JSON。装法任选:

```bash
winget install jqlang.jq
# 或 scoop install jq / choco install jq
```

**缺 jq 时 hook 会静默失效** —— 同样是「看起来正常但没在保护」。

### 必须在 Git Bash 里跑 Claude Code

`.claude/hooks/` 下是 `.sh` 脚本。**在 PowerShell 里跑 Claude Code,防护 hook 不生效**,而且不会有任何提示。

用 **Git Bash** 或 **WSL**。

---

## 第 4 步:跑起 demo

```bash
cd demo
pnpm install
pnpm run dev
```

### ⚠️ 必须用 pnpm,不要用 npm

依赖树由 `demo/pnpm-lock.yaml` 锁定。没装 pnpm:`npm i -g pnpm`

**在这个项目里跑 `npm install` 会报一个和真实原因毫无关系的错:**

```
npm error Cannot read properties of null (reading 'matches')
```

它既不提 pnpm 也不提源,**极容易被误判成「网络连不上」而去折腾 registry —— 换源、加超时全都没用,因为根本不是网络问题**。改用 `pnpm install` 即可。

> 顺带一提:公司内网源 `depend.iflytek.com` 确实偶尔 SSL 握手失败(`SSL_ERROR_SYSCALL`),但那是另一回事,且只在 pnpm 也装不动时才需要考虑。

demo 是**标准效果的唯一参照**:只要按下面方式正确接入,你项目里的显示效果与 demo 分毫不差。

---

## 第 5 步:在你的业务项目里接入(四步,缺一不可)

> 完整说明见根目录 **`接入指南.md`**,这里只列要点。
> **第 2 小步漏了主色会不对、第 3 小步漏了规则完全不生效,且都不报错。**

### ① 引入样式三层(顺序固定,错了覆盖会失效)

在你项目的 `main.ts`:

```ts
import 'element-plus/dist/index.css'                    // ① EP 基础
import '<path>/design-spec/design-token/index.scss'      // ② 设计令牌
import '<path>/design-spec/el-theme/index.scss'          // ③ 组件覆盖层
```

范本见 `demo/src/main.ts`。**只引这两个 index 入口**,不要逐个引内部文件。

> ⛔ **严禁把 `design-token/` 或 `el-theme/` 拷贝一份进你自己的仓库。**
> 拷贝 = 脱离源头,源头更新它不动。只能通过引用(相对路径 / submodule / monorepo)指向同一份。

### ② 品牌色:默认就是品牌绿,**通常什么都不用做**

接入三层样式后,主按钮、选中态、hover 自动是品牌绿 `#23B283`,与 demo 一致。

只有**要换成别的品牌色**时才需要注册,方法见 `接入指南.md` 第 2 步。

### ③ ⭐ 引入规则(最容易漏,漏了等于没接设计系统)

在**你自己业务项目根**的 `CLAUDE.md` 里加一行:

```
@<相对路径>/design-spec/CLAUDE.md
```

**为什么必须**:第 ① 步只给你**外观**。不加这行,你项目的 Claude Code **看不到任何规则** —— 不知道做弹窗要用 `el-dialog`、不知道表单要套 form-pattern、不知道 2 个选项该用 Radio 而不是 Select。它只会拿到样式,然后凭手感发明布局。

加了这行,Claude Code 启动时会自动把整份规范拉进上下文,做表单/表格/弹窗时自动去读对应规则。

### ④ 用组件

基础组件直接用 Element Plus,样式已被接管:

```vue
<el-button type="primary">主操作</el-button>
```

业务组件从统一入口 import:

```ts
import { PageFrame, StepBar, Breadcrumb, DataTable, AiButton, UserAvatar, SearchMini }
  from '<path>/design-spec/components'
```

---

## 快速自查清单

| ✅ | 事项 | 漏了会怎样 |
|---|---|---|
| ☐ | 解压后跑第 0 步三条自查 | `.claude` 丢了,防护静默失效 |
| ☐ | 装 jq | hook 静默失效 |
| ☐ | 在 Git Bash 里跑 Claude Code | hook 静默失效 |
| ☐ | `sh .githooks/install.sh` | catalog 推旧的,全员扩展面板过期 |
| ☐ | 用 pnpm 不用 npm | 报一个看不懂、和网络无关的错 |
| ☐ | 业务项目根 `CLAUDE.md` 加 `@` 那行 | 只有外观没有规则,等于没接设计系统 |

> 注意上面**大部分问题都不报错**,只是静默失效。所以这些自查值得花那几分钟。

---

## 有问题看哪里

| 想知道什么 | 看哪个文件 |
|---|---|
| **完整接入说明**(唯一入口文档) | 根目录 `接入指南.md` |
| 设计规范全文(令牌 / 组件 / 设计模式) | `design-spec/CLAUDE.md` |
| 防护 hook 是干嘛的、被拦了怎么办 | `.claude/hooks/README.md` |
| 设计系统整体结构 | `设计系统结构讲解.html`(浏览器打开) |
| 各组件标准效果 | 跑起 demo 自己点 |
