---
author: myjin2
updated: 2026-04-12
---

# 设计系统规范

---

## ⛔ 最高铁律：一切变动必须使用已定义的设计令牌与组件

> **本节优先级高于本文件其余所有内容。** 后续在本项目中的**任何**代码 / 样式 / 页面变动，都必须遵守：

1. **所有样式值必须走已定义的设计令牌（design token）** —— 颜色、间距、圆角、字体、阴影、动效等，一律引用 `design-spec/design-token/` 里定义的 CSS 变量（如 `var(--iflyv-brand-primary)`、`var(--iflyv-radius-sm)`、`font: var(--iflyv-font-...)` 等）。**禁止**写裸值（硬编码 hex 色、`px` 魔法间距、字面字号 / 行高 / 字重等）。找不到对应令牌时，先停下来向用户确认应新增哪个令牌，而不是就地硬编码。

2. **凡涉及组件，必须使用已定义的设计组件** —— 优先复用 Element Plus + `design-spec/el-theme/` 主题覆盖层里已定义的组件与样式规范，不得另起炉灶手写一套等价组件或覆盖其既有样式。需要的组件不存在时，先向用户确认，而不是临时拼装。

3. **组件样式的唯一数据源在 `el-theme/components/`（Single Source of Truth）** —— 这是本设计系统作为"可被多项目引用的组件库"能成立的根基，**绝对不可破坏**：

   - **唯一定义处**：每个组件的视觉样式（外观 / 尺寸 / 字体字重 / 颜色 / 选中禁用等状态 / 装饰 / 交互态）**只允许**定义在 `design-spec/el-theme/components/<组件>.scss` 全局层。这里是"源头"，一个组件只有一份定义。
   - **使用方只"引用"不"复制"**：任何使用方（本仓库的 `demo/`、以及将来 `import` 本组件库的其它项目）一律通过约定 class（如 `.tabs-page` / `.tabs-sub` / 裸 `el-tabs` 等）或直接用 EP 组件来**引用**全局标准，**严禁**在使用方的 `<style scoped>`（含 `:deep()`）或任何局部样式里**重新定义 / 覆盖组件外观**。局部 scoped 只能放"纯本页排版留白"（如某个 demo 块的 `margin` 间距），**不得触碰组件本身的外观规则**。
   - **为什么**：源头改一次 → 所有引用方随之同步变化（改品牌色、调圆角、换字重，一处生效、处处生效），这正是设计系统"单点维护、统一控制"的价值。一旦有人在使用方 scoped 里私自复制并改写组件样式（"局部私货"），该处就**脱离源头、不再同步**：你改全局它不动，将来统一升级时要挨个项目翻 scoped 排查 —— **这就是最典型的翻车**。
   - **判定"局部私货"（命中任一即违规，须归位到全局层）**：在使用方文件里出现 `:deep(.el-xxx...)` 覆盖了组件的字号 / 字重 / 颜色 / 边框 / 圆角 / 选中态 / 装饰 / 溢出行为等**外观类**属性；或用裸值 / 私有 class 复刻了某个已有标准组件的样子。
   - **正确做法**：需要调整组件外观 → 改 `el-theme/components/<组件>.scss` 源头；某个组合场景（如 Badge+Tabs）需要固定行为 → 也提炼进全局层并给出约定 class，而不是写死在单个 demo 里。拿不准该不该进全局时，先向用户确认，不要就地 scoped 覆盖。

4. **引用方接入方式（决定"同步"是否成立的前提）** —— "改源头 → 引用方同步"只在引用方**正确引用了全局层**时才成立。任何使用方（本仓库 `demo/`、以及将来接入的其它项目）必须按以下方式接入，否则会脱钩、不再同步：

   - **必须引入三层，且顺序固定**（见 `el-theme/index.scss` 头部注释）：① `element-plus/dist/index.css`（EP 基础）→ ② `design-token/index.scss`（令牌统一入口）→ ③ `el-theme/index.scss`（组件覆盖层统一入口）。顺序错了覆盖会失效。范本见 `demo/src/main.ts`。
   - **只引 index 入口,不逐个引子文件**：引用方引 `design-token/index.scss` 和 `el-theme/index.scss` 这两个统一入口即可，内部 48 个组件文件由 index 汇总，引用方无需也不应逐个引用。
   - **严禁"拷贝式"接入**：绝对不要把 `design-token/` 或 `el-theme/` 的 scss **拷贝一份**进引用方自己的仓库——拷贝 = 脱离源头,源头更新它不动。只能通过引用（相对路径 / npm 包 / submodule / monorepo 共享）指向同一份源头。
   - **引用方内部不得写组件外观私货**：接入后，引用方自己项目里同样受本铁律约束——不在自己的 scoped / 局部样式里覆盖组件外观（见本条上文"局部私货"判定）。
   - **由此得出**：对"正确接入（引全三层 + 不拷贝 + 不写私货）"的项目，**`demo/` 的显示效果就是它们引用后的标准真实效果，分毫不差**；对拷贝式 / 漏引 / 魔改式接入的项目，不保证同步——那是接入方式错误，不是机制问题。
   - **⭐ 规则接入（决定"下游 CC 能否自动应用规范"的关键一步，必做，一次性一行、永久生效）**：

     > 样式三层（上面那些 `.scss`）只让下游拿到**外观**；要让下游项目的 **CC 也自动遵守设计模式 / 组件用法 / 令牌纪律 /「任务→必读」触发指针**，下游**必须**在其**项目根 `CLAUDE.md`** 里加一行 `@` 引入本文件。**不加这一行，下游 CC 看不到本规范**——它只会拿到样式、却不知道"做弹窗要用 el-dialog、做表单要套 form-pattern、时间要走 formatTime"等规则。

     在下游项目根 `CLAUDE.md` 里加（路径按接入方式选一种）：
     ```
     # 相对路径 / submodule / monorepo（直接指向源头）
     @../xiaoya3.0设计规范/design-spec/CLAUDE.md
     # npm 包接入
     @node_modules/<包名>/design-spec/CLAUDE.md
     ```
     **原理**：Claude Code 启动时加载下游项目根 `CLAUDE.md`，顺着 `@` 递归把本文件全文拉进**下游 CC 的上下文**——于是下游 CC 做表单/表格/弹窗/页面时，自动命中本文件的「任务→必读」指针，按标准组织布局、用令牌、用标准组件。**范本见本仓库根 `CLAUDE.md`**（它自己就用 `@design-spec/CLAUDE.md` 接了，可直接照抄改路径）。

     规则仍单一数据源在本文件，**源头更新 → 所有已 `@` 接入的下游随之生效**，无需下游改任何东西。

5. **落地前自检** —— 每次改动完成前，逐条核对上述各点；命中「找不到令牌 / 找不到组件 / 想在使用方 scoped 覆盖组件外观 / 引用方接入方式不合规」时必须暂停并向用户说明，由用户决定新增全局定义还是调整方案。

6. **改一处 = 扫全部引用，主动同步（不待用户追问）** —— 改任何令牌值 / 组件规则 / 设计模式规则时，**必须主动扫描并同步所有引用它的地方**，不留「规范说 A、代码是 B」的裂缝。典型的联动面：
   - 改**令牌/组件间距值** → 同步 `el-theme/*.scss`（实现）+ `references/foundations.md`（间距/圆角速查）+ 相关 `*-pattern.md`（若引用了该值）。
   - 改**设计模式规则** → 同步该模式的 `*-pattern.md`（规则正文 + 反例 + 可照抄骨架 三处口径一致）+ 对应 demo 展示。
   - 改**组件外观** → demo 与源头一致性（见「删 demo 时源头如何同步」类规则）。
   > 这是**主动纪律**，不依赖用户发现不一致才修。改规则前先想「哪些地方引用了它」，改完逐一核对。

本设计系统分两层，各有其源头目录与规矩：

| 层 | 源头位置 | 本质 | 规矩所在 |
|---|---|---|---|
| **基础组件层** | `el-theme/components/*.scss` | 原生 EP + SCSS 皮肤覆盖（换外观，行为归 EP） | 上文最高铁律 |
| **业务组件层** | `design-spec/components/<Name>/*.vue` | 用基础组件搭出的成品（AI 面板、卡片等），有自己的 DOM | **`design-spec/components/CLAUDE.md`** |

> **接到「新增 / 修改业务组件」类任务前，必须先读 `design-spec/components/CLAUDE.md`**（业务组件层公约：三条铁律 + 自由发挥 vs 私货判据 + 正规调用方式）。业务组件层是最高铁律的延伸而非例外——用到基础组件只引用不覆盖、token 尽量复用不符合先确认、只装扮自己骨架不碰基础组件外观。

---

## 🎛 配置式组件设计范式（基础组件 / 业务组件通用）

> 设计组件时，**能力优先用「正交配置项」建模，而非穷举「组合类型」**。这是本设计系统组件的统一范式，新增或改造组件时都按此思路。

### 核心两条（最高优先级）

1. **一定改源头** —— 组件的任何外观/交互/配置能力，只定义在源头（基础组件 `el-theme/components/*.scss`、业务组件 `design-spec/components/<Name>`）。改源头 → 所有接入项目自动同步。**绝不在使用方 scoped 里写组件私货**（同最高铁律）。

2. **傻瓜式复用** —— 接入方用起来必须**成本最低、无需读实现**：
   - 能力尽量走 EP 原生 prop（`type` / `disabled` / `loading` 等），传属性即生效。
   - 必须自定义时，用**一个稳定的约定 class**（如按钮下拉箭头的 `.btn-caret`），接入方只写约定好的那一行，间距/动效/状态排除等全在源头处理好。
   - **在源头文件顶部写「接入方速查」注释**（范本见 `el-theme/components/button.scss` 顶部）：把所有能力的最简用法列出来，接入方复制即用，不必理解内部实现。

### 建模原则：正交配置项 > 组合类型

- **区分「类型」与「配置项」**：互斥选一的是类型（次/主/危险、实心/文本）；可自由叠加的是配置项（带图标、带下拉箭头……）。
- **配置项不穷举成具名类型**：N 个配置项就是 N 个正交开关，自由组合。**严禁**为每种组合定义具名类型（否则 `6类 × 有无图标 × 有无箭头 = 24 种` 具名类型爆炸）。
- **源头只需保证各配置项独立 + 能正交叠加**（如箭头 `vertical-align:middle` 保证与图标共存不错位），组合自动成立，无需为组合写任何东西。

### demo 展示约定（带配置项的组件）

- **配置开关放右侧**，用 **`background: var(--iflyv-bg-card)`** 卡片包裹，卡片加「配置项」标题。
- 开关用 `el-switch`，实时控制左侧「类型 × 状态」网格的配置叠加（范本见 `demo/src/components/ButtonDemo.vue`）。
- 网格只演示「类型 × 状态」的固定矩阵；配置项由开关动态叠加，**不再为每种配置单独铺一行**。

---

## 📐 设计模式（令牌 / 组件之上的组织规则）——【任务触发：必读细则】

> **这是令牌、组件之外的第三层规则**：不管"用什么值"（令牌）和"用什么控件"（组件），而管**"如何组织布局、如何工程化地组合使用组件"**。这一层解决的是「组织形式一致性」——即设计师在任意接入项目里说"帮我做一个表单/表格/列表页"，CC 都能按同一套标准组织，而非每次重新发明布局。
>
> **每个模式细则在 `references/patterns/` 下，严格按四段式写：何时用（触发）/ 强制做法 / 反例 / 可照抄骨架。**

### ⚡ 任务 → 必读细则（命中即先 Read，再动手）

| 当任务是… | 动手前必须先 Read | 说明 |
|---|---|---|
| 做**表单** / 录入界面 / 一组「标签+控件」的信息组织 | `references/patterns/form-pattern.md` | 表单布局范式 |
| 做**表格** / **列表** / **信息流**（一行=一条记录：表头/单元格类型、状态列、操作列、卡片条目…） | `references/patterns/list-item-pattern.md` | 列表条目模式：一条记录=四区拼装（全局功能/主题/关键信息/操作）+ 每类元素用哪个组件 |
| 做**工具栏** / 页面头部操作条 / 列表页顶部一排（标题 + tab + 筛选下拉 + 搜索 + 操作按钮） | `references/patterns/toolbar-pattern.md` | 工具栏布局：固定顺序（标题→组件级tab→下拉→搜索→次按钮→主按钮）+ 左右分配三分支（有标题→标题左其余右 / 无标题有筛选→筛选左按钮右 / 都无→按钮左）+ 间距 spacing-3、内边距 spacing-4/spacing-6 |
| 加**搜索框** / 搜索入口 / 列表筛选检索 | `references/patterns/search-pattern.md` | 搜索框选型（核心→展开 / 非核心→收起）+ SearchMini 用法 |
| **从固定选项里选一个**：加**单选 / 下拉选择器** / 筛选下拉 / 类型选择 | `references/patterns/select-pattern.md` | **先选控件**：>5 不建议 Radio→用 Select；≤5 需并排比较时 Radio 更好（非必须，也可 Select）。用 Select 再定可清除（有默认语义项→不可清 / 无默认值→可清）+ 可搜索 |
| 做**弹窗** / 操作载体选型（一个填写/操作/确认，纠结弹窗 vs 页面 vs 抽屉） | `references/patterns/dialog-pattern.md` | 载体四维判据（信息量少 / 关联度高 / 任务连贯性弱 / 页面遮挡多 → 用弹窗）+ 宽度三档场景 |
| 做**弹窗** / **对话框** / 模态框（"弹个框做表单/确认/警示…"） | `references/component-interaction.md`（Dialog 段） | 一律用 `<el-dialog>`（禁手撸、禁 ElMessageBox）；内容放**默认插槽=content 区**、按钮放 `#footer`；危险操作加 `class="is-danger"` 等 4 种语义变体；**宽度按场景从三档选**（确认/单字段=400、常规表单=640、复杂/双列=800），**禁非档位宽度**（如 `width="480px"` 越界）。弹窗内若含表单/表格/搜索，**再叠加对应模式**（如表单→form-pattern） |
| 做**抽屉** / 侧边浮层 / Drawer（纠结抽屉 vs 对话框 vs 页面） | `references/component-interaction.md`（Drawer 段） | 先按四维判据（承载信息量 / 信息关联度 / 任务连贯性 / 页面遮挡）判断该用抽屉还是对话框：**信息量大 + 任务需来回切换主页面 → 抽屉**；信息量少 + 需全神贯注 → 对话框。抽屉宽度 400px（轻量）或 600px（详细），有按钮时放 `#footer`（主按钮在右，与对话框一致） |
| 做**二次确认** / 删除·离开前确认 / 就地气泡确认 | `references/component-interaction.md`（Popconfirm 段） | 低风险操作用 `el-popconfirm`（就地气泡，不打断），高风险 / 需全神贯注才升级 `el-dialog`。**气泡内按钮走 32px 小号（源头已定，下游不覆盖）**：直接用 `<el-button>` 即自动 32px，禁给气泡按钮硬写 `height` / `size="small"` 凑小号；按钮个数「一退路 + 一进路」（取消 + 确认） |
| 用 **Tag 标签** / 状态标签 / 一处并列多个彩色标签（状态列、卡片头…） | `references/component-interaction.md`（Tag 段） | 一处并列标签**≤3 个为宜**，避免 4 个及以上平铺让标签退化成噪音（枚举全部状态色板的展示页除外） |
| 做**下拉菜单** / 更多操作 / 悬浮菜单 / 一个触发后弹出的选项面板 | `references/component-interaction.md`（Dropdown 段） | 一律用 `el-dropdown`（面板放 `#dropdown` 插槽、禁手撸浮层）；**触发器按场景选形态**（主操作→按钮 / 紧凑操作列→纯图标 / 行内轻量入口→文字+箭头），不写死单一形态 |
| 做**面包屑** / 路径导航 / 带返回箭头的层级导航 | `references/component-interaction.md`（Breadcrumb 段） | 一律用业务组件 `Breadcrumb`（`design-spec/components/`），传 `items` + 接 `@back`（真实项目通常接 `router.back()`），禁手写 `el-breadcrumb`+`span` 拼返回箭头；无上一级传 `back-disabled`，不需要箭头传 `show-back=false` |
| 做**步骤条** / 步骤指示器 / 分步流程进度（"第几步 / 填写→生成→完成"这类多步骤流程头部） | `components/StepBar/StepBar.vue`（业务组件，顶部速查注释） | 一律用业务组件 `StepBar`（`design-spec/components/`），`import { StepBar } from '<path>/design-spec/components'`；传 `:steps="['填写要求','生成清单','生成内容']"`（数组长度即步骤数）+ `:current="2"`（当前步 1-based，< 为已完成、= 为当前、> 为未开始）。大圆节点+补零序号+渐变粗连接线+前进/回退动效全在源头，**禁手写 div 拼步骤条、禁用 el-steps 复刻这套观感** |

> 命中上表任一场景时，**这是强制步骤，不是可选参考**：先读对应细则，按其中的「强制做法」执行、避开「反例」、可直接套用「可照抄骨架」。读之前不要凭直觉自拟布局。
>
> 新增设计模式时：① 在 `references/patterns/` 建 `<模式>-pattern.md`（四段式）；② 在本表加一行「任务→必读」映射。**两步都做，模式才算沉淀。**

### demo 展示约定（设计模式 tab 的统一版式）

每个设计模式在 demo「设计模式」tab 的展示，**统一为上下两块 `bg-card` 大卡片**（范本见 `demo/src/components/pattern/PatternFormDemo.vue`）：

- **① 模式规则**：用**可视化拆解图**讲通用设计规则（面向设计师，非代码）。做法：顶部一排功能区标签（不同色胶囊）→ 引导线 → 一条真实样式示例条 → 每区一句规则说明，各行按 grid 列对齐。**不出现任何代码/API 术语**（那些在 `*-pattern.md` 里给 CC 读）。
- **② 标准范例**：一个真实、可交互的标准实现，作对照。
- **命名统一**：左侧导航名 = 页面标题名，格式「英文 中文」（如 `Form Layout 表单布局`），与基础组件命名风格一致。详见下方「demo 文案的英文层级约定」。
- 卡片 padding `spacing-6`、圆角 `radius-lg`、两卡间距 `spacing-6`，全走令牌。

#### demo 文案的英文层级约定（哪层双语 / 哪层只中文 / 哪层是功能标识不可删）

demo 里出现的英文分三层，性质不同，处理方式不同——**新增 demo 时按此把握，不要在小节标题冒出多余英文，也不要误删组件层的英文**：

| 层级 | 例 | 英文角色 | 约定 |
|---|---|---|---|
| **左侧导航名 / 页面大标题**（`demo-section__title`） | `Checkbox 多选`、`Form Layout 表单布局` | 组件/模式的**规范命名**（国际化标准名，便于对照 EP 文档） | **保留双语「英文 中文」**——这是命名规范，不可省英文 |
| **小节内标题**（`demo-label`，页面内某个演示块的小标题） | 应写 `多选` / `开关` / `分页`，**不是** `Checkbox 多选` | 纯展示文案，上层大标题已给过规范名，此处重复 = 冗余 | **只用中文**，不加英文前缀 |
| **组件 class / API / prop**（`.el-checkbox`、`type`、`disabled`；scss 选择器与注释里的 EP 名） | `.el-timeline`、`el-button type="danger"` | 组件的**功能标识 / 对外接口**，样式与逻辑靠它生效 | **原样保留，绝不删**——删了组件直接失效，这是命脉不是文案 |

> 一句话：**规范命名层双语、小节文案层只中文、组件功能层是代码不能碰。** 删 demo 冗余英文时只动"小节文案层"，另两层不许波及。

---

## ✍ 文案规范（文字 / 时间 / 数字怎么写）——【任务触发：必读细则】

> 令牌管"用什么值"、组件管"用什么控件"、设计模式管"怎么组织布局"，**文案规范管"文字/时间/数字怎么写、怎么展示"**。这一层解决"展示口径一致性"——同一种数据（时间、金额、状态词…）在任意接入项目里都按同一套写法呈现。
>
> **文案规范的规则细则在 `references/copywriting/` 下，按四段式写：何时用（触发）/ 格式规则 / 强制做法 / 反例 / 可照抄骨架。可执行实现在 `design-spec/utils/`（如 `format-time.ts`），规则文档与 demo 都引用同一份实现——单一数据源。**

### ⚡ 任务 → 必读细则（命中即先 Read，再动手）

| 当任务是… | 动手前必须先 Read | 可执行实现 |
|---|---|---|
| 展示**时间 / 时间戳 / 日期**（创建时间、更新时间、时间线、卡片时间…） | `references/copywriting/time.md` | `utils/format-time.ts` 的 `formatTime` |

> 命中上表任一场景时，**这是强制步骤**：先读细则，按「格式规则」定形态、import 对应「可执行实现」落地、避开「反例」。**绝不硬编码文案值**（如把时间写死成 `2026-03-17`）。
>
> 新增文案规范时：① 在 `references/copywriting/` 建 `<主题>.md`（四段式）；② 若有可执行逻辑，在 `design-spec/utils/` 建实现，规则文档与所有使用方都 import 它；③ 在本表加一行「任务→必读」映射。**三步都做，文案规范才算沉淀（可被下游识别、不散落硬编码）。**

---

## ⚠ 强制工作流（接到任何页面设计 / 改造任务时，必须按顺序执行）

> **本节是命令式流程，不是参考资料。** 每一步都必须显式完成并输出结论，不得跳过或在内心默认。

### Step 1 — 项目级确认（首次设计前询问一次，后续复用）

检查当前项目是否已完成品牌色确认。若未确认，向用户提问：

| 确认项 | 询问内容 | 缺省值 |
|--------|---------|--------|
| 品牌色 | 该系统使用哪个品牌色？ | `default`（Green） |

确认结果记录后，同一项目内所有页面直接复用。

---

## Design Context

### Users
高校教师、教学助理、学生、督导员。教师是核心用户，日常在备课、授课、批改、评测等场景中高频使用。使用环境多为办公室或教室，常在多任务并行中操作平台，需要快速完成任务、减少认知负担。学生端侧重作业提交、课堂互动和学情查看。

### Brand Personality
**专业 · 高效 · 清晰 · 智慧**

- **专业**：工具可靠、信息准确，让教师信赖
- **高效**：操作路径短，信息层级清晰，减少无效点击
- **清晰**：界面结构一目了然，不需要学习成本
- **智慧**：AI 能力自然融入工作流，不喧宾夺主

情绪目标：让用户感到**专业可靠**和**简洁克制**——平台是安静的助手，不打扰教学本身。

### Aesthetic Direction

**视觉基调：** 飞书/Notion 式的干净效率工具风格——信息密度适中，留白充裕，结构清晰。

**现有设计语言：**
- 品牌绿 `#23b283` 仅用于主操作按钮和关键强调，用色极为克制

**参考方向：** 飞书、Notion — 干净、高效、信息密度适中的效率工具风格

**反面参考（绝对避免）：**
- 花哨炫技 — 过多动效、渐变、装饰元素
- 老旧过时 — 灰暗沉闷的传统政务系统感
- 信息过载 — 密密麻麻的功能堆砌

### Design Principles

1. **内容优先，装饰有度** — 界面是内容的容器，视觉元素应服务于信息传达。装饰不是禁区，但每一处装饰都应有其存在的理由，避免无意义的视觉堆砌。
2. **层级即导航，线条为辅** — 优先通过间距、字重、色彩层级引导视线；线条不排斥，但应尽量减少使用，必要时用极细分割线辅助区分。
3. **绿主蓝辅，避免大面积绿** — 品牌绿 `#23b283` 是主色，蓝色 `#0077ff` 是辅色，两者配合使用。避免页面中出现大范围绿色，主色用于关键操作和强调点，辅色用于信息类和 AI 相关场景。状态色（红/橙）仅用于语义表达。
4. **AI 有温度但不抢戏** — AI 功能用柔和的蓝色渐变和插画风格做视觉区分，传达智慧感但不脱离整体克制基调。
5. **一致性高于创意** — 同类页面必须使用相同的布局模式、间距规则和交互范式。可预测性比新鲜感更重要。

---

## 三、设计规范文件结构

```
design-spec/
├── CLAUDE.md                        ← 本文件（设计哲学 + 路由）
├── references/
│   ├── foundations.md                ← 必读：令牌 + 硬规则 + 配色 + 动效
│   ├── efficiency-guide.md          ← 效率型：布局 + 视觉 + 组件原则
│   ├── display-guide.md             ← 展示型：排版 + 装饰 + 光斑 + 数据可视化 + GSAP 动效 + 沉浸式背景
│   ├── component-interaction.md     ← EP 组件交互规范
│   ├── patterns/                    ← 设计模式细则（四段式，见「设计模式」小节）
│   └── copywriting/                 ← 文案规范细则（四段式，见「文案规范」小节）
│       └── time.md                   ← 通用时间：本年省年 / 跨年带年 / 不带秒（实现 utils/format-time.ts）
├── utils/                           ← 文案规范等的可执行实现（单一数据源，使用方 import 不重写）
│   └── format-time.ts                ← 通用时间格式化 formatTime（time.md 与所有时间展示处共用）
├── design-token/                    ← 设计令牌（CSS 变量）
│   ├── index.scss
│   ├── css/palette.scss             ← 基础色板（Sass 变量 + CSS 变量 + accent 别名）
│   ├── css/semantic.scss            ← 亮色语义层（brand/bg/text/icon/border/shadow...）
│   ├── css/palette-dark.scss        ← 暗色色板
│   ├── css/semantic-dark.scss       ← 暗色语义覆盖 + surface 描边工具类
│   ├── css/spacing.scss             ← 间距 + 圆角
│   ├── css/brands.scss              ← 品牌色注册 mixin
│   ├── css/font.scss                ← 字体族 + 语义化字体变量
│   ├── css/animation.scss           ← 过渡/动画变量
│   └── font/douyin_num.otf
├── el-theme/                        ← Element Plus 主题覆盖层
│   ├── index.scss
│   ├── var-mapping.scss
│   ├── assets/                      ← 静态资源（tab装饰、空状态插图等）
│   └── components/                  ← 40+ 组件样式覆盖
```
